# API Documentation

Empty by design in Phase 1 — no API surface exists yet. Populated once the first Route Handlers/Server Actions land (Phase 2+), following the conventions in [ARCHITECTURE.md](../../ARCHITECTURE.md) §31–32 (consistent errors, `/api/v1/` versioning).
