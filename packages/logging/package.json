{
  "name": "@erp/logging",
  "private": true,
  "version": "0.0.0",
  "description": "Structured logging interface (request_id, correlation_id, tenant_id, module, operation, duration, status)",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "typescript": "^5.9.3",
    "vitest": "^4.1.11"
  }
}
