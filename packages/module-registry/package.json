{
  "name": "@erp/module-registry",
  "private": true,
  "version": "0.0.0",
  "description": "ModuleManifest contract and dependency-graph validation (cycle detection) — no database/framework dependency",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "test": "vitest run"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^26.2.0",
    "typescript": "^5.9.3",
    "vitest": "^4.1.11"
  }
}
