{
  "name": "@erp/auth",
  "private": true,
  "version": "0.0.0",
  "description": "Session/token layer on audited primitives — password hashing, session issuance/verification/revocation, rate limiting",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "test": "vitest run"
  },
  "dependencies": {
    "@erp/database": "workspace:*",
    "@node-rs/argon2": "^2.1.0",
    "@oslojs/encoding": "^1.1.0",
    "drizzle-orm": "^0.45.2"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "typescript": "^5.9.3",
    "vitest": "^4.1.11"
  }
}
