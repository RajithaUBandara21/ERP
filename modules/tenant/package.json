{
  "name": "@erp/tenant",
  "private": true,
  "version": "0.0.0",
  "description": "Tenant lifecycle module — tenant profile, resolution, and database provisioning use cases",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "test": "vitest run"
  },
  "dependencies": {
    "@erp/database": "workspace:*",
    "@erp/logging": "workspace:*",
    "@erp/module-registry": "workspace:*",
    "@erp/validation": "workspace:*",
    "drizzle-orm": "^0.45.2"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "postgres": "^3.4.9",
    "typescript": "^5.9.3",
    "vitest": "^4.1.11"
  }
}
