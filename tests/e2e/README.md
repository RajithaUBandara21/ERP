# tests/e2e

Cross-cutting end-to-end tests spanning multiple modules (the workflow chain in [TESTING.md](../../TESTING.md) §2: create tenant → login → install module → ... → generate report).

Not yet implemented. Individual steps are added as each phase makes them possible, starting with tenant creation in Phase 3.
